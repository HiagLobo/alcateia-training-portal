[build]
  command = "pnpm install && pnpm exec vite build --root client && esbuild server/_core/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist"
  publish = "client/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
